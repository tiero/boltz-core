const Errors = {
  TAPROOT_NOT_SUPPORTED: 'taproot not supported',
  ONLY_NATIVE_SEGWIT_INPUTS: 'only native SegWit inputs supported',
  INCONSISTENT_BLINDING: 'all or none inputs and outputs have to be blinded',
};

export default Errors;
